import 'package:flutter_test/flutter_test.dart';
import 'package:app/modules/maindrawer/maindrawer_page.dart';

void main() {
  testWidgets('MaindrawerPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MaindrawerPage(title: 'Maindrawer')));
    //  final titleFinder = find.text('Maindrawer');
    //  expect(titleFinder, findsOneWidget);
  });
}
