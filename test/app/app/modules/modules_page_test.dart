import 'package:flutter_test/flutter_test.dart';
import 'package:app/app/modules/modules_page.dart';

void main() {
  testWidgets('ModulesPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ModulesPage(title: 'Modules')));
    //  final titleFinder = find.text('Modules');
    //  expect(titleFinder, findsOneWidget);
  });
}
